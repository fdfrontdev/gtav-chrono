# lib/

`ScriptHookVDotNet3.dll` is **gitignored** (third-party binary, license forbids redistribution).

Restore it after cloning:
```
copy D:\games\gtav\ScriptHookVDotNet3.dll lib\
```
Required by `src/Chrono.Boundary/Chrono.Boundary.csproj` (HintPath reference).
Version: v1.1.0.6 (Enhanced fork) — must match the game folder's copy.
